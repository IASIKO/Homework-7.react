import React, { useContext } from "react";
import { ListContext } from "./ListContext";

const List = () => {
    const { setToggleOut } = useContext(ListContext);
    return (
      <div>
       
      </div>
    );
  };
  
  export default List;